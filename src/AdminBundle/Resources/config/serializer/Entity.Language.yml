AdminBundle\Entity\Language:
    exclusion_policy: ALL
    properties:
        id:
            expose: true
            groups: [default, viewable, communication_model]

        name:
            expose: true
            groups: [default, viewable, communication_model]

        listDescription:
            expose: true
            groups: [default, viewable, communication_model]
            serialized_name: description

        showOnPage:
            expose: true
            groups: [default, viewable, communication_model]

        images:
            expose: true
            groups: [viewable]

        createdAt:
            expose: true
            groups: [default, communication_model]
            type: DateTime<'Y-m-d H:m:s'>

        updatedAt:
            expose: true
            groups: [default, communication_model]
            type: DateTime<'Y-m-d H:m:s'>

