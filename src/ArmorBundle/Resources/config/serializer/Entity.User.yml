ArmorBundle\Entity\User:
    exclusion_policy: all
    properties:
        id:
            expose: true
            type: int
            groups: [default, communication_model]

        name:
            expose: true
            type: string
            groups: [default, communication_model]

        lastname:
            expose: true
            type: string
            groups: [default, communication_model]

        username:
            expose: true
            type: string
            groups: [default, communication_model]

        currentLanguageSession:
            expose: true
            type: ArmorBundle\Entity\LanguageSession
            groups: [default]

        enabled:
            expose: true
            type: bool
            groups: [default, communication_model]

        createdAt:
            expose: true
            groups: [default, communication_model]
            type: DateTime<'Y-m-d H:m:s'>

        updatedAt:
            expose: true
            groups: [default, communication_model]
            type: DateTime<'Y-m-d H:m:s'>
