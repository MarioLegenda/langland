import React from 'react';
import {Form} from './gameForm.jsx';

export class GameInit extends React.Component {
    constructor(props) {
        super(props);
    }

    render() {
        return (
            <Form/>
        )
    }
}